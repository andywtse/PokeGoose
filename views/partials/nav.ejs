  <% if (!user) { %>
    <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img src="/assets/pokeball-pokemon.svg" alt="" width="30" height="24">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link <%- title === 'Home' ? 'active' : '' %>" href="/">Home</a>
          <a class="nav-link <%- title === 'Community' ? 'active' : '' %>" href="/community">Community</a>
          <a class="nav-link <%- title === 'Teams' ? 'active' : '' %>" href="/teams">Teams</a>
        </div>
        <div class="navbar-nav ms-auto">
          <% if (user) { %>
            <a href="/profiles" class="nav-link"> Profile</a>
            <a href="/auth/logout" class="nav-link">
              <button class="btn btn-outline-success me-2" type="button">Logout</button>
            </a>
          <% } else { %>
            <a href="/auth/google" class="nav-link siwg-link">
              <button type="submit" class="btn btn-sm btn-outline-secondary siwg">
                <img id="siwg-image" alt="Sign in with Google" />
              </button>
            </a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>